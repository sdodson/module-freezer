apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: module-freezer
  namespace: module-freezer
  labels:
    app: module-freezer
spec:
  selector:
    matchLabels:
      app: module-freezer
  template:
    metadata:
      labels:
        app: module-freezer
    spec:
      serviceAccountName: module-freezer
      hostPID: false
      tolerations:
        - operator: Exists
      containers:
        - name: module-freezer
          image: quay.io/module-freezer/module-freezer:latest
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
            - name: POLL_INTERVAL
              value: "30"
            - name: REMOVE_TAINT_ON_RESOLVE
              value: "false"
            - name: LOCK_MODULES
              value: "false"
          volumeMounts:
            - name: proc
              mountPath: /host/proc
              readOnly: true
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              memory: 64Mi
          securityContext:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
      volumes:
        - name: proc
          hostPath:
            path: /proc
